#include <stdio.h>
#include<math.h>
// Modulus Operator % in C

int main() {
    /* Modulus operator '%' is used to find the remainder of integer division in C language
       Important rule: Works only with integers (int). 
    */
    printf("%d", 17%10);
    /* Answer is : 7 
       Because: 17 ÷ 10 = 1 and remainder: 7 (So answer is : 7)
    */
    return 0;

}

